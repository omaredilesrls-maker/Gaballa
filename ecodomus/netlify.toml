# Config del sito EcoDomus (secondo sito nello stesso repo).
# In Netlify: crea un NUOVO sito, imposta "Base directory" = ecodomus
[build]
  publish = "."
  functions = "functions"

[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404
